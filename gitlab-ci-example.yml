stages:
  - test

check_wordpress:
  stage: test
  image: docker:26.1.0
  services:
    - docker:26.1.0-dind

  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""

  script:
    - apk add --no-cache curl
    - docker compose up -d
    - sleep 15
    - docker ps | grep wordpress
    - docker exec wordpress-docker-lab-wordpress-1 curl -f http://localhost
    - docker compose down
